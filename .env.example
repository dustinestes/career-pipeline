# Clockify credentials for THIS repo (not shared with other checkouts).
# Create a key: Clockify → Preferences → Advanced → Manage API Keys
# Prefer a dedicated workspace (e.g. GitHub) so public package work stays grouped.
CLOCKIFY_API_KEY=
CLOCKIFY_WORKSPACE_ID=
