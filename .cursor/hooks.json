{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "prompt",
        "timeout": 10,
        "prompt": "Clockify: call clockify_get_running_timer. If a timer is running and inactivity is past the .clockify.yml threshold (45 minutes), stop it or ask the user. Fail open if Clockify MCP is unavailable."
      }
    ]
  }
}
